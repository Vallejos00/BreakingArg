{{>header}}
<h1>Perfil</h1>
<div class="form-container-miPerfil">
    
    <form method="POST" action="/miPerfil/delete">
        <label class="lbl-title" for="firstName">Nombre:</label>
            <p class="form-input">{{user.firstName}}</p>

         <label class="lbl-title" for="lastName">Apellido:</label>
            <p class="form-input">{{user.lastName}}</p>

         <label class="lbl-title" for="email">Email</label>
            <p class="form-input">{{user.email}}</p>
        
         <label class="lbl-title" for="userName">Nombre de usuario</label>
            <p class="form-input">{{user.userName}}</p>

</div>

  
    <div class="btn-container">
       <a class="btn" href="/foro">Volver</a>
       <a class="btn" href="/miPerfil/editProfile">Editar perfil</a>




   <input type="checkbox" id="btn-modal">
   <label for="btn-modal" class="lbl-modal">Eliminar perfil</label>

    <div class="modal">
        <div class="modal-container"> 
          <div class="modal-content">
        <p>¿Seguro que quiere eliminar su perfil?</p>
        <div class="btn-modal-container">
    <label for="btn-modal" class="btn-modal">Cancelar</label>
    <input type="submit" class="btn-delete" value="Eliminar" id="btn-delete">
       </div>
          </div>
        </div>
    </div>

</div>


</form>


{{>footer}}
